import React from "react";
import * as Component from "@/app/component";

export const CounterContainer: React.FC = () => {

    return (
        <section>
            <Component.Hook.View.Content.Counter />
        </section>
    )
}